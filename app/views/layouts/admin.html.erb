<!DOCTYPE html>
<html>
  <head>
    <title>Payin Admin</title>
    <meta
      name="viewport"
      content="minimum-scale=1, initial-scale=1, width=device-width, user-scalable=no, shrink-to-fit=no"
    />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= javascript_packs_with_chunks_tag 'admin' %>
    <%= favicon_link_tag "favicon.png" %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
